// Use the npm @types/leaflet package for Leaflet global types in this project.
// Deno will resolve this via the import map in `deno.json` (see "imports").
/// <reference types="npm:@types/leaflet" />

// Use the npm @types/leaflet package for Leaflet global types in this project.
// Deno will resolve this via the import map in `deno.json` (see "imports").
/// <reference types="npm:@types/leaflet" />

export {};
